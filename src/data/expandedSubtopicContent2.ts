import type { SubtopicContent } from '@/types';

export const EXPANDED_SUBTOPIC_CONTENT_2: Record<string, SubtopicContent> = {

  // ═══════════════════════════════════════════════════
  // FULLSTACK / FRONTEND — HTML & CSS
  // ═══════════════════════════════════════════════════
  'st-html-1': {
    title: 'HTML Structure',
    definition: 'HTML (HyperText Markup Language) structures web pages using elements represented by tags that define content semantics.',
    explanation: 'Every HTML page has a basic structure: DOCTYPE declaration, html, head (metadata), and body (visible content). Elements are nested to create a tree-like structure. Block elements (div, section, p) take full width; inline elements (span, a, strong) take only needed space.',
    example: '<!DOCTYPE html>\n<html>\n  <head>\n    <title>My Page</title>\n  </head>\n  <body>\n    <h1>Welcome</h1>\n    <p>This is a paragraph.</p>\n  </body>\n</html>',
    keyTakeaways: ['HTML uses tags to structure content on web pages.', 'The basic structure is DOCTYPE, html, head, body.', 'Block elements take full width; inline elements take only needed space.', 'Elements nest to form a tree structure.'],
    commonMistakes: ['Forgetting to close tags properly.', 'Not using the DOCTYPE declaration.', 'Putting visible content in the head section.'],
    whenToUse: 'HTML is the foundation of every web page — use it to structure all web content.',
    interviewTip: 'Mention that you always use semantic HTML5 elements (header, nav, main, article, section, footer) for accessibility and SEO.',
    tryYourself: 'Create a basic HTML page with a heading, paragraph, and a link.',
  },
  'st-html-2': {
    title: 'Semantic HTML',
    definition: 'Semantic HTML uses elements that convey meaning about their content, like header, nav, article, section, aside, and footer.',
    explanation: 'Semantic elements describe their purpose to browsers, screen readers, and developers. Instead of using div for everything, use header for page top, nav for navigation, main for primary content, article for self-contained content, section for thematic grouping, aside for side content, and footer for page bottom.',
    example: '<header>\n  <nav>Navigation links</nav>\n</header>\n<main>\n  <article>\n    <h1>Blog Post</h1>\n    <p>Content...</p>\n  </article>\n</main>\n<footer>Copyright 2024</footer>',
    keyTakeaways: ['Semantic elements describe the meaning of content, not just appearance.', 'Use header, nav, main, article, section, aside, footer instead of generic divs.', 'Semantic HTML improves accessibility and SEO.', 'Screen readers use semantic elements to navigate pages.'],
    commonMistakes: ['Using div for everything instead of semantic elements.', 'Nesting semantic elements incorrectly (e.g., footer inside header).', 'Using section when article is more appropriate.'],
    whenToUse: 'Always prefer semantic HTML5 elements over generic divs for better accessibility and SEO.',
    interviewTip: 'Mention that semantic HTML is essential for WCAG accessibility compliance — a common interview topic.',
    tryYourself: 'Convert a div-based layout to use semantic HTML5 elements.',
  },
  'st-html-3': {
    title: 'Forms & Inputs',
    definition: 'HTML forms collect user input using form elements like input, textarea, select, and button with various input types.',
    explanation: 'Forms use the <form> element containing input controls. Common input types: text, email, password, number, date, checkbox, radio, file. The label element associates text with inputs for accessibility. Required, placeholder, and pattern attributes add validation. Form submission sends data to a server endpoint.',
    example: '<form action="/submit" method="POST">\n  <label for="email">Email:</label>\n  <input type="email" id="email" name="email" required>\n  <label for="pwd">Password:</label>\n  <input type="password" id="pwd" name="pwd" minlength="8">\n  <button type="submit">Submit</button>\n</form>',
    keyTakeaways: ['Forms collect user input via input, textarea, select elements.', 'Always use label elements for accessibility.', 'Input types include text, email, password, number, date, checkbox, radio.', 'Required, placeholder, and pattern attributes add built-in validation.'],
    commonMistakes: ['Not associating labels with inputs using for/id attributes.', 'Forgetting to set the form method and action attributes.', 'Not using appropriate input types (e.g., text instead of email).'],
    whenToUse: 'Use forms whenever you need to collect structured user input.',
    interviewTip: 'Mention that you always use label elements with for attributes for accessibility — a common best practice.',
    tryYourself: 'Create a registration form with email, password, and name fields, including labels and validation.',
  },
  'st-html-4': {
    title: 'Accessibility Basics',
    definition: 'Web accessibility means designing websites that are usable by people with disabilities, following WCAG guidelines.',
    explanation: 'Key accessibility practices include using semantic HTML, providing alt text for images, ensuring sufficient color contrast, making all functionality keyboard-accessible, using ARIA attributes when needed, and structuring content with proper heading hierarchy. Screen readers rely on these to convey content to visually impaired users.',
    example: '<img src="chart.png" alt="Sales increased 20% from Q1 to Q2 2024">\n<button aria-label="Close menu">×</button>\n<a href="#main">Skip to main content</a>\n<h1>Page Title</h1>\n<h2>Section Heading</h2>',
    keyTakeaways: ['Always provide alt text for images.', 'Ensure keyboard navigation works for all interactive elements.', 'Use proper heading hierarchy (h1 → h2 → h3).', 'ARIA attributes supplement semantic HTML when needed.'],
    commonMistakes: ['Using alt="" on informative images (should describe the image).', 'Skipping heading levels (e.g., h1 to h3).', 'Not making interactive elements keyboard-accessible.'],
    whenToUse: 'Apply accessibility best practices in every web project — it\'s a legal requirement in many contexts.',
    interviewTip: 'Mention WCAG compliance levels (A, AA, AAA) — most organizations target AA compliance.',
    tryYourself: 'Audit a web page for accessibility issues and fix at least three common problems.',
  },
  'st-css-1': {
    title: 'Selectors & Specificity',
    definition: 'CSS selectors target HTML elements for styling. Specificity determines which rule applies when multiple rules conflict.',
    explanation: 'Common selectors: element (p), class (.btn), ID (#header), descendant (div p), pseudo-class (:hover), pseudo-element (::before). Specificity is calculated as (inline, ID, class, element). ID has higher specificity than class, which is higher than element. When conflicts arise, the more specific rule wins.',
    example: 'p { color: blue; }           /* element selector */\n.text { color: red; }        /* class selector */\n#title { color: green; }     /* ID selector */\ndiv p { font-size: 16px; }  /* descendant selector */\n.btn:hover { background: gray; } /* pseudo-class */',
    keyTakeaways: ['Selectors target elements: element, .class, #id, descendant, pseudo-class.', 'Specificity: inline > ID > class > element.', 'Use classes as the primary styling mechanism, not IDs.', '!important overrides specificity but should be avoided.'],
    commonMistakes: ['Using IDs for styling (too specific, hard to override).', 'Overusing !important to fix specificity conflicts.', 'Not understanding specificity calculation leads to confusing style bugs.'],
    whenToUse: 'Use selectors to target elements for styling. Understand specificity to resolve style conflicts.',
    interviewTip: 'Explain that you prefer class-based styling over IDs for reusability and lower specificity.',
    tryYourself: 'Create a CSS ruleset using element, class, and ID selectors, then test which wins in a conflict.',
  },
  'st-css-2': {
    title: 'Box Model',
    definition: 'The CSS box model treats every element as a rectangular box with content, padding, border, and margin layers.',
    explanation: 'The box model has four layers: content (text/image), padding (space inside the border), border (line around padding), and margin (space outside the border). box-sizing: border-box includes padding and border in the element\'s width, making sizing more predictable. The default is content-box, which adds padding and border to the specified width.',
    example: '.box {\n  width: 200px;\n  padding: 20px;\n  border: 2px solid black;\n  margin: 10px;\n  box-sizing: border-box; /* total width stays 200px */\n}',
    keyTakeaways: ['Every element is a box with content, padding, border, and margin.', 'box-sizing: border-box includes padding and border in the width.', 'Margin is space outside; padding is space inside the border.', 'Margins of adjacent elements can collapse (overlap).'],
    commonMistakes: ['Not setting box-sizing: border-box, causing unexpected element sizes.', 'Confusing margin (outside) with padding (inside).', 'Not accounting for margin collapse in vertical spacing.'],
    whenToUse: 'Understanding the box model is essential for all CSS layout work.',
    interviewTip: 'Mention that you always set box-sizing: border-box globally — it\'s a standard reset practice.',
    tryYourself: 'Create a box with padding, border, and margin, then toggle box-sizing to see the difference.',
  },
  'st-css-3': {
    title: 'Flexbox',
    definition: 'Flexbox is a CSS layout module for arranging elements in one dimension (row or column) with flexible sizing and alignment.',
    explanation: 'Set display: flex on a container to make its children flex items. Key properties: justify-content (main axis alignment), align-items (cross axis alignment), flex-direction (row or column), gap (spacing between items), flex-wrap (allow wrapping). Flex items can grow, shrink, and set basis with the flex shorthand.',
    example: '.container {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  gap: 16px;\n}\n.item { flex: 1; } /* grow equally */',
    keyTakeaways: ['Flexbox arranges items in one dimension (row or column).', 'justify-content aligns on the main axis; align-items on the cross axis.', 'flex: 1 makes items grow equally to fill space.', 'gap adds consistent spacing between flex items.'],
    commonMistakes: ['Confusing justify-content (main axis) with align-items (cross axis).', 'Not setting flex-direction when switching from row to column.', 'Using flex: 1 without understanding grow, shrink, and basis.'],
    whenToUse: 'Use Flexbox for one-dimensional layouts: navbars, card rows, button groups, centering content.',
    interviewTip: 'Mention that you use Flexbox for 1D layouts and CSS Grid for 2D layouts — knowing when to use each is key.',
    tryYourself: 'Create a navbar with a logo on the left and links on the right using Flexbox.',
  },
  'st-css-4': {
    title: 'CSS Grid',
    definition: 'CSS Grid is a two-dimensional layout system for creating complex grid-based layouts with rows and columns.',
    explanation: 'Set display: grid on a container. Define columns with grid-template-columns and rows with grid-template-rows. Use fr units for flexible sizing, gap for spacing, and grid-column/grid-row to span items across multiple cells. Grid excels at full-page layouts and dashboard designs.',
    example: '.layout {\n  display: grid;\n  grid-template-columns: 200px 1fr;\n  grid-template-rows: auto 1fr auto;\n  gap: 16px;\n}\n.header { grid-column: 1 / -1; } /* span all columns */',
    keyTakeaways: ['CSS Grid creates two-dimensional layouts with rows and columns.', 'Use fr units for flexible, proportional sizing.', 'grid-column and grid-row can span multiple cells.', 'Grid is ideal for full-page layouts and dashboards.'],
    commonMistakes: ['Using Grid for simple one-dimensional layouts where Flexbox is better.', 'Not understanding fr units vs fixed units.', 'Forgetting that grid items can overlap if positioned incorrectly.'],
    whenToUse: 'Use CSS Grid for two-dimensional layouts: page layouts, dashboards, photo galleries.',
    interviewTip: 'Explain that Grid and Flexbox are complementary — Grid for page structure, Flexbox for component-level layouts.',
    tryYourself: 'Create a basic page layout with header, sidebar, main content, and footer using CSS Grid.',
  },
  'st-css-5': {
    title: 'Responsive Design',
    definition: 'Responsive design makes web pages adapt to different screen sizes using media queries, flexible units, and mobile-first thinking.',
    explanation: 'Media queries apply different styles based on screen width: @media (max-width: 768px). Use relative units (rem, %, vw/vh) instead of fixed pixels. Mobile-first design starts with mobile styles and adds desktop styles via min-width media queries. The viewport meta tag is essential for responsive behavior on mobile devices.',
    example: '/* Mobile-first */\n.card { width: 100%; }\n@media (min-width: 768px) {\n  .card { width: 50%; }\n}\n@media (min-width: 1024px) {\n  .card { width: 33.33%; }\n}',
    keyTakeaways: ['Media queries apply styles based on screen size breakpoints.', 'Use relative units (rem, %, vw/vh) for flexible sizing.', 'Mobile-first design starts small and adds larger screen styles.', 'Always include the viewport meta tag for mobile rendering.'],
    commonMistakes: ['Using fixed pixel widths that break on mobile.', 'Not testing on actual mobile devices.', 'Starting with desktop styles and removing for mobile (harder to maintain).'],
    whenToUse: 'Use responsive design for every web project — users access sites on various devices.',
    interviewTip: 'Mention mobile-first design — it\'s the industry standard approach to responsive web design.',
    tryYourself: 'Create a responsive card grid that shows 1 column on mobile, 2 on tablet, and 3 on desktop.',
  },
  'st-css-6': {
    title: 'Transitions',
    definition: 'CSS transitions smoothly animate property changes over a specified duration, creating subtle visual feedback.',
    explanation: 'Transitions interpolate between property values. Use transition-property, transition-duration, transition-timing-function, and transition-delay. Common properties to transition: color, background, transform, opacity, width, height. Avoid transitioning layout properties like width/height for performance — use transform instead.',
    example: '.btn {\n  background: blue;\n  transition: background 0.3s ease, transform 0.2s ease;\n}\n.btn:hover {\n  background: darkblue;\n  transform: scale(1.05);\n}',
    keyTakeaways: ['Transitions animate property changes smoothly.', 'Specify property, duration, timing function, and delay.', 'Transform and opacity are the most performant properties to animate.', 'Avoid transitioning width/height/margin for performance.'],
    commonMistakes: ['Transitioning layout properties (width, height) causing reflows.', 'Not specifying a duration (defaults to 0s, no visible transition).', 'Using transitions for state changes that should be instant.'],
    whenToUse: 'Use transitions for hover states, focus states, and subtle UI feedback.',
    interviewTip: 'Mention that you prefer transform and opacity for animations because they\'re GPU-accelerated.',
    tryYourself: 'Add a smooth color and scale transition to a button hover state.',
  },
  'st-css-7': {
    title: 'Keyframe Animations',
    definition: 'CSS keyframe animations define multi-step animations using @keyframes, allowing complex motion without JavaScript.',
    explanation: '@keyframes defines animation steps using percentages (0% to 100% or from/to). Apply with animation-name, animation-duration, animation-iteration-count, animation-timing-function, and animation-direction. Common use cases: loading spinners, fade-ins, slide-ins, and pulse effects.',
    example: '@keyframes fadeIn {\n  from { opacity: 0; transform: translateY(20px); }\n  to { opacity: 1; transform: translateY(0); }\n}\n.element {\n  animation: fadeIn 0.5s ease forwards;\n}',
    keyTakeaways: ['@keyframes defines multi-step animation sequences.', 'Use animation-name, duration, iteration-count, timing-function.', 'Common patterns: fadeIn, slideIn, pulse, spin.', 'animation-fill-mode: forwards keeps the final state.'],
    commonMistakes: ['Not setting animation-fill-mode, causing the element to snap back.', 'Using too many simultaneous animations (performance issues).', 'Not using prefers-reduced-motion media query for accessibility.'],
    whenToUse: 'Use keyframe animations for loading states, entrance effects, and continuous motion.',
    interviewTip: 'Mention prefers-reduced-motion media query — it shows you care about accessibility in animations.',
    tryYourself: 'Create a loading spinner using @keyframes and the rotate transform.',
  },
  'st-css-8': {
    title: 'Transforms',
    definition: 'CSS transforms modify element appearance without affecting layout — translate, rotate, scale, and skew.',
    explanation: 'Transforms are GPU-accelerated and don\'t trigger reflow, making them ideal for animations. translate() moves, rotate() rotates, scale() resizes, and skew() distorts. Combine transforms with transitions for smooth effects. transform-origin changes the pivot point.',
    example: '.card {\n  transition: transform 0.3s ease;\n}\n.card:hover {\n  transform: translateY(-5px) scale(1.02);\n}\n.spin { transform: rotate(45deg); transform-origin: top left; }',
    keyTakeaways: ['Transforms are GPU-accelerated — great for performance.', 'translate, rotate, scale, skew are the main transform functions.', 'Combine multiple transforms in one declaration.', 'transform-origin changes the pivot point for rotation/scaling.'],
    commonMistakes: ['Using margin/top/left for animation instead of transform.', 'Not understanding transform-order (operations apply right to left).', 'Forgetting to set transform-origin for specific pivot points.'],
    whenToUse: 'Use transforms for hover effects, entrance animations, and any visual motion.',
    interviewTip: 'Explain that transforms are GPU-accelerated and don\'t cause reflow — a performance best practice.',
    tryYourself: 'Create a card that lifts up and slightly scales on hover using transforms.',
  },
  'st-css-9': {
    title: 'Typography',
    definition: 'Web typography controls font family, size, weight, line height, and spacing to make text readable and visually appealing.',
    explanation: 'Key properties: font-family (with fallbacks), font-size (use rem for accessibility), line-height (1.5 for body text), letter-spacing, font-weight, and text-align. Use system fonts for performance or Google Fonts with font-display: swap. Limit to 2-3 font families for consistency.',
    example: 'body {\n  font-family: "Inter", -apple-system, sans-serif;\n  font-size: 1rem;\n  line-height: 1.5;\n  color: #333;\n}\nh1 { font-size: 2.5rem; font-weight: 700; line-height: 1.2; }',
    keyTakeaways: ['Use rem units for font sizes (respects user preferences).', 'Line-height of 1.5 for body, 1.2 for headings.', 'Always include font fallbacks in font-family.', 'Limit to 2-3 font families per project.'],
    commonMistakes: ['Using px for font sizes (doesn\'t respect user accessibility settings).', 'Too many font families creating visual clutter.', 'Not setting line-height, making text hard to read.'],
    whenToUse: 'Typography choices affect every text element — set them early in a project.',
    interviewTip: 'Mention that you use rem units for accessibility — it respects browser font-size settings.',
    tryYourself: 'Create a typography scale using rem units for h1, h2, h3, body, and small text.',
  },
  'st-css-10': {
    title: 'Color Theory',
    definition: 'Color theory in web design uses color relationships, contrast, and psychology to create visually appealing and accessible interfaces.',
    explanation: 'Use CSS color formats: hex (#3b82f6), rgb/rgba, hsl/hsla. Ensure WCAG contrast ratios (4.5:1 for normal text, 3:1 for large text). Use CSS custom properties (variables) for consistent theming. Limit your palette to a primary, secondary, and accent color plus neutrals.',
    example: ':root {\n  --primary: #3b82f6;\n  --bg: #ffffff;\n  --text: #1a1a1a;\n}\n.btn { background: var(--primary); color: white; }\n/* Contrast ratio: 4.5:1+ for body text */',
    keyTakeaways: ['Use CSS custom properties for consistent color theming.', 'Ensure WCAG contrast ratios (4.5:1 for normal text).', 'Limit to a primary, secondary, accent, and neutral colors.', 'HSL is useful for generating color variations.'],
    commonMistakes: ['Low contrast text that\'s hard to read.', 'Using too many colors creating visual noise.', 'Not testing color contrast for accessibility.'],
    whenToUse: 'Apply color theory principles when designing any user interface.',
    interviewTip: 'Mention WCAG color contrast requirements — it shows accessibility awareness.',
    tryYourself: 'Create a color system using CSS variables with a primary, secondary, and neutral palette.',
  },
  'st-css-11': {
    title: 'Layout Principles',
    definition: 'Layout principles guide the arrangement of elements: visual hierarchy, alignment, spacing, balance, and whitespace.',
    explanation: 'Visual hierarchy uses size, color, and position to guide attention. Alignment creates order and connection. Consistent spacing (using an 8px grid) creates rhythm. Whitespace (negative space) prevents clutter and improves focus. Balance can be symmetrical or asymmetrical.',
    example: '/* 8px spacing system */\n.card { padding: 24px; margin-bottom: 16px; gap: 8px; }\n/* Visual hierarchy */\nh1 { font-size: 2.5rem; }\nh2 { font-size: 1.75rem; }\np { font-size: 1rem; }',
    keyTakeaways: ['Visual hierarchy guides the eye through size, color, and position.', 'Use an 8px spacing system for consistent rhythm.', 'Whitespace prevents clutter and improves focus.', 'Alignment creates visual order and connection.'],
    commonMistakes: ['Inconsistent spacing creating visual disorder.', 'No clear visual hierarchy making everything look equally important.', 'Not using enough whitespace, creating cluttered designs.'],
    whenToUse: 'Apply layout principles in every design — they\'re the foundation of good UI.',
    interviewTip: 'Mention the 8px spacing system — it\'s used by Material Design and most design systems.',
    tryYourself: 'Create a card component using an 8px spacing system with clear visual hierarchy.',
  },

  // ═══════════════════════════════════════════════════
  // FULLSTACK — React
  // ═══════════════════════════════════════════════════
  'st-react-1': {
    title: 'Components & JSX',
    definition: 'React components are reusable UI pieces written in JSX — a syntax extension that lets you write HTML-like code in JavaScript.',
    explanation: 'Components are JavaScript functions that return JSX. JSX looks like HTML but is compiled to JavaScript. Components receive props as input and return elements as output. Function components are the modern standard. Components must return a single root element (or use fragments).',
    example: 'function Welcome({ name }) {\n  return <h1>Hello, {name}!</h1>;\n}\n// Usage\n<Welcome name="Alice" />',
    keyTakeaways: ['Components are JavaScript functions that return JSX.', 'JSX looks like HTML but compiles to JavaScript.', 'Components receive props and return UI elements.', 'Components must return a single root element (or fragment).'],
    commonMistakes: ['Forgetting to capitalize component names (React treats lowercase as DOM elements).', 'Not wrapping multiple elements in a parent or fragment.', 'Using class instead of className in JSX.'],
    whenToUse: 'Use components to break down any UI into reusable pieces.',
    interviewTip: 'Mention that you use function components with hooks — class components are legacy.',
    tryYourself: 'Create a simple Profile component that takes name and age as props and displays them.',
  },
  'st-react-2': {
    title: 'Props',
    definition: 'Props are read-only data passed from parent to child components, enabling data flow and component reuse.',
    explanation: 'Props are passed as attributes on JSX elements and received as a function parameter. They are read-only — child components cannot modify them. For dynamic data, props can include functions (callbacks) that the child calls to communicate back to the parent. Destructuring props is the common pattern.',
    example: 'function UserCard({ name, email, onEdit }) {\n  return (\n    <div>\n      <h3>{name}</h3>\n      <p>{email}</p>\n      <button onClick={onEdit}>Edit</button>\n    </div>\n  );\n}',
    keyTakeaways: ['Props pass data from parent to child components.', 'Props are read-only — children cannot modify them.', 'Pass callback functions as props for child-to-parent communication.', 'Destructure props for cleaner code.'],
    commonMistakes: ['Trying to modify props inside the child component.', 'Not destructuring props, making code verbose.', 'Passing too many props — consider context or composition instead.'],
    whenToUse: 'Use props for parent-to-child data flow in React applications.',
    interviewTip: 'Mention that props are read-only by design — it enforces unidirectional data flow, making apps predictable.',
    tryYourself: 'Create a parent component that passes user data and an edit callback to a child component.',
  },
  'st-react-3': {
    title: 'State & useState',
    definition: 'State is internal data that changes over time. useState is a React hook that adds state to function components.',
    explanation: 'useState returns a state value and a setter function. Calling the setter triggers a re-render with the new state. State updates are asynchronous and batched. For updates based on previous state, use the functional form: setCount(prev => prev + 1). Never mutate state directly — always use the setter.',
    example: 'function Counter() {\n  const [count, setCount] = useState(0);\n  return (\n    <button onClick={() => setCount(count + 1)}>\n      Count: {count}\n    </button>\n  );\n}',
    keyTakeaways: ['useState returns [value, setter] pair.', 'State updates are async and batched.', 'Use functional updates when new state depends on old: setX(prev => ...).', 'Never mutate state directly — always use the setter.'],
    commonMistakes: ['Mutating state directly instead of using the setter.', 'Using state for values that don\'t need re-renders (use useRef or useMemo).', 'Not using functional updates when state depends on previous value.'],
    whenToUse: 'Use useState for any data that changes and should trigger a re-render.',
    interviewTip: 'Mention that state updates are batched in React 18 — multiple setStates in one event handler result in one re-render.',
    tryYourself: 'Create a counter with increment, decrement, and reset buttons using useState.',
  },
  'st-react-4': {
    title: 'Event Handling',
    definition: 'React event handling uses synthetic events that wrap native browser events, providing consistent behavior across browsers.',
    explanation: 'React events use camelCase (onClick, onChange, onSubmit) instead of lowercase. Event handlers receive a SyntheticEvent. Prevent default behavior with e.preventDefault(). For events that need the current value of an input, use e.target.value. Pass functions (not function calls) as event handlers.',
    example: 'function Form() {\n  const [text, setText] = useState("");\n  return (\n    <input\n      value={text}\n      onChange={(e) => setText(e.target.value)}\n    />\n  );\n}',
    keyTakeaways: ['React events use camelCase: onClick, onChange, onSubmit.', 'Event handlers receive a SyntheticEvent object.', 'Use e.preventDefault() to stop default behavior.', 'Pass the function reference, not a function call.'],
    commonMistakes: ['Calling the function instead of passing it: onClick={handleClick()} instead of onClick={handleClick}.', 'Not calling e.preventDefault() in form submissions.', 'Not understanding that React events differ slightly from native DOM events.'],
    whenToUse: 'Use event handlers for all user interactions: clicks, input changes, form submissions.',
    interviewTip: 'Mention that React\'s synthetic events normalize browser differences — you don\'t need cross-browser workarounds.',
    tryYourself: 'Create a form with an input and submit button that logs the input value on submit.',
  },
  'st-react-5': {
    title: 'useEffect',
    definition: 'useEffect is a React hook for managing side effects — API calls, subscriptions, timers, and DOM manipulation — after render.',
    explanation: 'useEffect takes a function and an optional dependency array. With no array, it runs after every render. With an empty array [], it runs once after mount. With specific dependencies [x, y], it runs when those change. The cleanup function returned from useEffect runs before the next effect or unmount.',
    example: 'useEffect(() => {\n  fetch("/api/data")\n    .then(res => res.json())\n    .then(setData);\n}, []); // run once on mount\n\nuseEffect(() => {\n  const timer = setInterval(tick, 1000);\n  return () => clearInterval(timer); // cleanup\n}, []);',
    keyTakeaways: ['useEffect manages side effects after render.', 'Empty dependency array [] runs once on mount.', 'Dependencies [x] re-run when x changes.', 'Return a cleanup function to clean up subscriptions and timers.'],
    commonMistakes: ['Forgetting the dependency array, causing infinite re-renders.', 'Not including all used variables in the dependency array.', 'Not cleaning up subscriptions and timers (memory leaks).'],
    whenToUse: 'Use useEffect for API calls, event subscriptions, timers, and any side effects.',
    interviewTip: 'Mention the exhaustive-deps lint rule — it catches missing dependencies that cause subtle bugs.',
    tryYourself: 'Create a component that fetches data from an API on mount and displays it.',
  },
  'st-react-6': {
    title: 'useRef',
    definition: 'useRef is a React hook that creates a mutable reference that persists across renders without triggering re-renders.',
    explanation: 'useRef returns an object with a .current property. Unlike state, changing ref.current does not cause a re-render. Common uses: accessing DOM elements directly, storing mutable values that don\'t need rendering, and keeping track of previous values. Refs are ideal for values that don\'t affect the UI.',
    example: 'function FocusInput() {\n  const inputRef = useRef(null);\n  useEffect(() => {\n    inputRef.current?.focus();\n  }, []);\n  return <input ref={inputRef} />;\n}',
    keyTakeaways: ['useRef creates a mutable reference that persists across renders.', 'Changing ref.current does NOT trigger a re-render.', 'Common uses: DOM access, storing mutable values, tracking previous values.', 'Access the value via ref.current.'],
    commonMistakes: ['Using useRef for values that should trigger re-renders (use useState instead).', 'Reading ref.current during render (should only read in effects or handlers).', 'Not initializing ref values properly.'],
    whenToUse: 'Use useRef for DOM access, timers, and values that don\'t need to trigger re-renders.',
    interviewTip: 'Explain the difference between useState and useRef — state triggers re-renders, refs don\'t.',
    tryYourself: 'Create a component that focuses an input on mount using useRef.',
  },
  'st-react-7': {
    title: 'useMemo & useCallback',
    definition: 'useMemo memoizes computed values to avoid recalculating on every render. useCallback memoizes functions to maintain referential equality.',
    explanation: 'useMemo caches a computed value and only recalculates when dependencies change. useCallback returns a memoized function that stays the same across renders unless dependencies change. Both are performance optimizations — use them when profiling shows unnecessary re-renders or expensive calculations.',
    example: 'const sortedData = useMemo(() => {\n  return data.sort((a, b) => a.name.localeCompare(b.name));\n}, [data]);\n\nconst handleClick = useCallback(() => {\n  console.log("clicked", count);\n}, [count]);',
    keyTakeaways: ['useMemo caches computed values between renders.', 'useCallback caches function references between renders.', 'Both take a dependency array to control when to recompute.', 'Use them for performance — not premature optimization.'],
    commonMistakes: ['Overusing useMemo/useCallback for trivial calculations (overhead exceeds benefit).', 'Not including all dependencies in the array.', 'Using them as the default instead of profiling first.'],
    whenToUse: 'Use useMemo for expensive calculations and useCallback for functions passed as props to memoized children.',
    interviewTip: 'Mention that premature memoization can hurt performance — always profile before optimizing.',
    tryYourself: 'Use useMemo to cache a sorted and filtered list so it doesn\'t recompute on every render.',
  },
  'st-react-8': {
    title: 'Custom Hooks',
    definition: 'Custom hooks are reusable functions that start with "use" and encapsulate stateful logic for sharing across components.',
    explanation: 'Custom hooks let you extract component logic into reusable functions. They can call other hooks internally. Naming convention: useSomething. They return values, functions, or objects. Custom hooks are the React way to share stateful logic without render props or HOCs.',
    example: 'function useFetch(url) {\n  const [data, setData] = useState(null);\n  const [loading, setLoading] = useState(true);\n  useEffect(() => {\n    fetch(url).then(r => r.json()).then(d => {\n      setData(d); setLoading(false);\n    });\n  }, [url]);\n  return { data, loading };\n}',
    keyTakeaways: ['Custom hooks start with "use" and encapsulate reusable logic.', 'They can call other hooks internally.', 'They return values, functions, or objects.', 'Custom hooks replace render props and HOCs for logic sharing.'],
    commonMistakes: ['Not starting the hook name with "use" (breaks linting rules).', 'Putting too much unrelated logic in one hook.', 'Not handling cleanup in effects within custom hooks.'],
    whenToUse: 'Use custom hooks when the same stateful logic is needed in multiple components.',
    interviewTip: 'Mention that custom hooks are the modern React pattern for sharing logic — cleaner than HOCs or render props.',
    tryYourself: 'Create a custom hook called useLocalStorage that persists state to localStorage.',
  },
  'st-react-9': {
    title: 'React Router',
    definition: 'React Router is the standard library for adding client-side routing to React applications, enabling navigation between pages.',
    explanation: 'React Router uses <BrowserRouter>, <Routes>, <Route>, and <Link> components. Routes define URL paths and their components. Link enables navigation without page reloads. Dynamic routes use URL parameters (/user/:id). The useParams hook accesses URL parameters. Nested routes allow complex layouts.',
    example: '<BrowserRouter>\n  <Routes>\n    <Route path="/" element={<Home />} />\n    <Route path="/user/:id" element={<UserProfile />} />\n  </Routes>\n</BrowserRouter>\n\n// In UserProfile\nconst { id } = useParams();',
    keyTakeaways: ['React Router enables client-side navigation without page reloads.', 'Use <Route> to map URLs to components.', 'Dynamic routes use :param syntax.', 'useParams, useNavigate, useLocation are key hooks.'],
    commonMistakes: ['Using <a> tags instead of <Link> (causes full page reloads).', 'Not wrapping the app in <BrowserRouter>.', 'Forgetting to handle 404 routes.'],
    whenToUse: 'Use React Router for any multi-page React application.',
    interviewTip: 'Mention that you use lazy loading with React Router for code-splitting — it improves initial load time.',
    tryYourself: 'Create a simple app with Home, About, and User Profile pages using React Router.',
  },
  'st-react-10': {
    title: 'Context API',
    definition: 'Context API is React\'s built-in solution for sharing data across components without prop drilling.',
    explanation: 'Create a context with createContext(). Wrap the app in a Provider with a value. Consumer components use useContext() to access the value. Context is ideal for themes, user authentication, and global settings. For complex state management, combine Context with useReducer.',
    example: 'const ThemeContext = createContext("light");\n\nfunction App() {\n  return (\n    <ThemeContext.Provider value="dark">\n      <Toolbar />\n    </ThemeContext.Provider>\n  );\n}\n\nfunction Toolbar() {\n  const theme = useContext(ThemeContext);\n  return <div className={theme}>Hello</div>;\n}',
    keyTakeaways: ['Context shares data globally without prop drilling.', 'createContext creates the context; Provider supplies the value.', 'useContext consumes the value in any child component.', 'Combine with useReducer for complex state management.'],
    commonMistakes: ['Overusing Context for all state (causes unnecessary re-renders).', 'Not splitting contexts for different concerns.', 'Creating a new context value object on every render.'],
    whenToUse: 'Use Context for global data like themes, auth, and user preferences that many components need.',
    interviewTip: 'Mention that Context is sufficient for most apps — you don\'t always need Redux.',
    tryYourself: 'Create a Theme context that toggles between light and dark mode.',
  },
  'st-react-11': {
    title: 'State Management Patterns',
    definition: 'State management patterns organize how state is structured, updated, and shared across a React application.',
    explanation: 'Common patterns: local state (useState for component-level), shared state (Context for cross-component), derived state (compute from props/state), and global state (Redux, Zustand for large apps). The reducer pattern (useReducer) handles complex state logic. Lifting state up to common parents is the fundamental React pattern.',
    example: '// Reducer pattern\nconst [state, dispatch] = useReducer(reducer, initialState);\n\n// Lifting state up\nfunction Parent() {\n  const [value, setValue] = useState("");\n  return <><ChildA value={value} /><ChildB onChange={setValue} /></>;\n}',
    keyTakeaways: ['Use local state for component-specific data.', 'Lift state to the nearest common parent for shared data.', 'Context for global concerns; Redux/Zustand for complex apps.', 'useReducer for complex state transitions.'],
    commonMistakes: ['Using Redux when Context would suffice.', 'Not lifting state up, causing duplicated state.', 'Storing derived state instead of computing it.'],
    whenToUse: 'Choose state management based on app complexity: local → Context → external library.',
    interviewTip: 'Mention that you start with local state, move to Context, and only add Redux when truly needed.',
    tryYourself: 'Create a simple useReducer-based state management for a todo list with add, toggle, and delete actions.',
  },
  'st-react-12': {
    title: 'Component Composition',
    definition: 'Component composition is the pattern of combining smaller components to build complex UIs, using children, slots, and render props.',
    explanation: 'Composition is "has-a" relationships instead of "is-a" inheritance. Use the children prop for flexible content injection. Render props pass a function as a prop to share rendering logic. Compound components (like <Select><Option/></Select>) create expressive APIs. Composition enables reusability without deep prop chains.',
    example: 'function Card({ children }) {\n  return <div className="card">{children}</div>;\n}\n// Usage\n<Card>\n  <h3>Title</h3>\n  <p>Content goes here</p>\n</Card>',
    keyTakeaways: ['Composition builds complex UIs from simple components.', 'The children prop enables flexible content injection.', 'Render props share rendering logic between components.', 'Compound components create expressive APIs.'],
    commonMistakes: ['Creating deeply nested component hierarchies instead of composing.', 'Not using children for flexible content.', 'Overusing render props when composition would be simpler.'],
    whenToUse: 'Use composition as the default pattern for building and reusing React components.',
    interviewTip: 'Mention that "composition over inheritance" is a core React principle — it avoids the prop drilling that inheritance-style patterns create.',
    tryYourself: 'Create a reusable Modal component using the children prop for flexible content.',
  },
  'st-react-13': {
    title: 'Error Boundaries',
    definition: 'Error boundaries are React components that catch JavaScript errors in child components, preventing the entire app from crashing.',
    explanation: 'Error boundaries use static getDerivedStateFromError and componentDidCatch (class components only). They catch errors during rendering, in lifecycle methods, and in constructors. They do NOT catch errors in event handlers, async code, or SSR. Wrap critical sections to show fallback UI instead of a blank screen.',
    example: 'class ErrorBoundary extends React.Component {\n  state = { hasError: false };\n  static getDerivedStateFromError() {\n    return { hasError: true };\n  }\n  render() {\n    if (this.state.hasError) return <h1>Something went wrong.</h1>;\n    return this.props.children;\n  }\n}',
    keyTakeaways: ['Error boundaries catch errors in child components during rendering.', 'They use getDerivedStateFromError and componentDidCatch.', 'They don\'t catch errors in event handlers or async code.', 'Wrap critical UI sections to show fallback UI.'],
    commonMistakes: ['Expecting error boundaries to catch event handler errors (they don\'t).', 'Not providing a useful fallback UI.', 'Wrapping the entire app in one boundary instead of granular ones.'],
    whenToUse: 'Use error boundaries around any component that might crash — especially with external data.',
    interviewTip: 'Mention react-error-boundary library — it provides error boundaries as hooks-friendly components.',
    tryYourself: 'Create an error boundary that catches errors and shows a friendly fallback message.',
  },
  'st-react-14': {
    title: 'Performance Optimization',
    definition: 'React performance optimization reduces unnecessary re-renders and improves load times using memoization, code-splitting, and virtualization.',
    explanation: 'Key techniques: React.memo for component memoization, useMemo/useCallback for expensive computations, React.lazy + Suspense for code-splitting, list virtualization (react-window) for large lists, and avoiding inline function/object creation in render. Use React DevTools Profiler to identify performance bottlenecks.',
    example: '// Memoize component\nconst ExpensiveList = React.memo(({ items }) => (\n  <ul>{items.map(i => <li key={i.id}>{i.name}</li>)}</ul>\n));\n\n// Code splitting\nconst Admin = React.lazy(() => import("./Admin"));',
    keyTakeaways: ['React.memo prevents re-renders when props don\'t change.', 'React.lazy + Suspense enables code-splitting.', 'Virtualize large lists with react-window or react-virtualized.', 'Profile with React DevTools before optimizing.'],
    commonMistakes: ['Premature optimization without profiling.', 'Using React.memo on components that always get new props.', 'Not using keys correctly in lists (causes unnecessary re-renders).'],
    whenToUse: 'Optimize performance when you notice slow renders, large lists, or slow initial load times.',
    interviewTip: 'Mention that you always profile with React DevTools before optimizing — measure, don\'t guess.',
    tryYourself: 'Use React.lazy and Suspense to code-split a route component.',
  },

  // ═══════════════════════════════════════════════════
  // FULLSTACK — Node.js
  // ═══════════════════════════════════════════════════
  'st-node-1': {
    title: 'Node.js Runtime',
    definition: 'Node.js is a JavaScript runtime built on Chrome\'s V8 engine that lets you run JavaScript outside the browser, primarily on servers.',
    explanation: 'Node.js uses an event-driven, non-blocking I/O model making it efficient for handling concurrent requests. It uses a single-threaded event loop instead of creating a thread per request. Node.js is ideal for I/O-heavy applications like APIs, real-time apps, and microservices. It\'s not suitable for CPU-intensive tasks.',
    example: 'const http = require("http");\nconst server = http.createServer((req, res) => {\n  res.writeHead(200);\n  res.end("Hello World");\n});\nserver.listen(3000);',
    keyTakeaways: ['Node.js runs JavaScript on the server using the V8 engine.', 'It uses a single-threaded, event-driven, non-blocking I/O model.', 'Ideal for I/O-heavy apps (APIs, real-time, microservices).', 'Not suitable for CPU-intensive computations.'],
    commonMistakes: ['Blocking the event loop with synchronous operations.', 'Using Node.js for CPU-heavy tasks (use worker threads or another language).', 'Not handling uncaught exceptions (crashes the process).'],
    whenToUse: 'Use Node.js for APIs, real-time apps, microservices, and any I/O-heavy server application.',
    interviewTip: 'Mention the event loop and non-blocking I/O — it\'s the core concept that makes Node.js efficient.',
    tryYourself: 'Create a simple HTTP server that responds with "Hello World" to all requests.',
  },
  'st-node-2': {
    title: 'Modules & npm',
    definition: 'Node.js uses CommonJS (require) or ES modules (import) for code organization. npm is the package manager for installing third-party libraries.',
    explanation: 'CommonJS uses require() and module.exports. ES modules use import/export. npm installs packages from the npm registry. package.json tracks dependencies. devDependencies are for development tools. npx runs packages without installing them globally. Semantic versioning (semver) manages version compatibility.',
    example: '// CommonJS\nconst express = require("express");\nmodule.exports = { myFunction };\n\n// ES Modules\nimport express from "express";\nexport function myFunction() {}',
    keyTakeaways: ['CommonJS uses require/module.exports; ES modules use import/export.', 'npm installs packages; package.json tracks dependencies.', 'devDependencies are for development tools only.', 'Use semver (^, ~) to control version updates.'],
    commonMistakes: ['Committing node_modules instead of using package-lock.json.', 'Not understanding the difference between dependencies and devDependencies.', 'Using ^ when you need exact versions (can break with updates).'],
    whenToUse: 'Use modules to organize code and npm to manage third-party dependencies.',
    interviewTip: 'Mention that you use ES modules for new projects — they\'re the modern standard.',
    tryYourself: 'Create a simple module that exports a function, then import and use it in another file.',
  },
  'st-node-3': {
    title: 'File System & Path',
    definition: 'The fs module provides file system operations (read, write, append, delete). The path module handles file path manipulation cross-platform.',
    explanation: 'fs has synchronous (readFileSync) and asynchronous (readFile) methods — always prefer async in production. Common operations: readFile, writeFile, appendFile, mkdir, readdir, unlink. The path module joins paths safely with path.join(), resolves relative paths with path.resolve(), and parses paths with path.parse().',
    example: 'const fs = require("fs").promises;\nconst path = require("path");\n\nasync function readConfig() {\n  const filePath = path.join(__dirname, "config.json");\n  const data = await fs.readFile(filePath, "utf-8");\n  return JSON.parse(data);\n}',
    keyTakeaways: ['Use fs.promises for async file operations.', 'Always prefer async over sync methods in production.', 'path.join() safely joins paths across platforms.', 'path.resolve() resolves relative to absolute paths.'],
    commonMistakes: ['Using sync fs methods in server code (blocks the event loop).', 'Not handling file operation errors.', 'Using string concatenation for paths instead of path.join().'],
    whenToUse: 'Use fs for reading configs, writing logs, and any file operations. Use path for cross-platform path handling.',
    interviewTip: 'Mention that you always use async fs methods and path.join() for cross-platform compatibility.',
    tryYourself: 'Write a script that reads a JSON config file and logs its contents.',
  },
  'st-node-4': {
    title: 'Events & Streams',
    definition: 'Events use the EventEmitter pattern for pub/sub. Streams handle reading/writing data in chunks, efficient for large files and network data.',
    explanation: 'EventEmitter lets objects emit named events that listeners subscribe to. Streams process data piece by piece instead of loading it all into memory. Readable streams (file reads, HTTP requests) produce data; Writable streams (file writes, HTTP responses) consume data. Pipes connect readable to writable streams.',
    example: 'const { EventEmitter } = require("events");\nconst emitter = new EventEmitter();\nemitter.on("greet", (name) => console.log(`Hello ${name}`));\nemitter.emit("greet", "Alice");\n\n// Streams\nconst { createReadStream } = require("fs");\ncreateReadStream("big.txt").pipe(process.stdout);',
    keyTakeaways: ['EventEmitter enables pub/sub pattern in Node.js.', 'Streams process data in chunks — memory efficient for large data.', 'Readable streams produce; Writable streams consume.', 'pipe() connects readable to writable streams.'],
    commonMistakes: ['Loading entire large files into memory instead of using streams.', 'Not removing event listeners (memory leaks).', 'Not handling stream error events.'],
    whenToUse: 'Use events for decoupled communication. Use streams for large files, HTTP bodies, and real-time data.',
    interviewTip: 'Mention that streams are what make Node.js efficient for large data — they process chunks, not whole files.',
    tryYourself: 'Create a script that reads a large file using streams and counts lines without loading it all into memory.',
  },
  'st-node-5': {
    title: 'Express Setup',
    definition: 'Express is a minimal Node.js web framework that simplifies building HTTP servers, routing, and middleware pipelines.',
    explanation: 'Express provides a simple API for creating servers: app.get(), app.post(), app.use() for middleware, and app.listen() to start. It handles routing, request/response objects, and middleware. Express is unopinionated — you choose your database, templating, and architecture.',
    example: 'const express = require("express");\nconst app = express();\napp.use(express.json());\napp.get("/", (req, res) => res.json({ msg: "Hello" }));\napp.listen(3000);',
    keyTakeaways: ['Express is a minimal, unopinionated web framework for Node.js.', 'app.get/post/put/delete define routes.', 'app.use() registers middleware.', 'express.json() parses JSON request bodies.'],
    commonMistakes: ['Not adding express.json() middleware for JSON bodies.', 'Not handling 404 and 500 routes.', 'Starting the server before defining routes.'],
    whenToUse: 'Use Express for any Node.js web server, API, or microservice.',
    interviewTip: 'Mention that Express is unopinionated — you choose your tools, which gives flexibility but requires discipline.',
    tryYourself: 'Create an Express server with a health check endpoint and a JSON POST route.',
  },
  'st-node-6': {
    title: 'Routing & Middleware',
    definition: 'Routing maps HTTP methods and URLs to handler functions. Middleware are functions that run in sequence before the route handler.',
    explanation: 'Express routes match method + path: app.get("/users/:id", handler). Middleware functions receive (req, res, next) and call next() to continue. Common middleware: logging, authentication, CORS, body parsing, error handling. Order matters — middleware runs in registration order.',
    example: '// Middleware\napp.use((req, res, next) => {\n  console.log(`${req.method} ${req.url}`);\n  next();\n});\n// Route with params\napp.get("/users/:id", (req, res) => {\n  res.json({ id: req.params.id });\n});',
    keyTakeaways: ['Routes match HTTP method + URL pattern.', 'Middleware runs in order before route handlers.', 'Middleware must call next() to continue the chain.', 'Route params use :param syntax, accessed via req.params.'],
    commonMistakes: ['Forgetting to call next() in middleware (request hangs).', 'Registering middleware after routes (it won\'t run).', 'Not handling errors in middleware (use error-handling middleware).'],
    whenToUse: 'Use routing for all API endpoints. Use middleware for cross-cutting concerns like auth, logging, and CORS.',
    interviewTip: 'Mention that middleware order matters — error handlers must be registered last.',
    tryYourself: 'Create a logging middleware that logs all requests and a route with a URL parameter.',
  },
  'st-node-7': {
    title: 'Request/Response Cycle',
    definition: 'The request-response cycle is the flow from receiving an HTTP request, through middleware and routing, to sending the response.',
    explanation: 'Express receives a request, runs middleware in order, matches a route, executes the handler, and sends the response. The req object contains method, url, headers, body, params, query. The res object has methods: json(), send(), status(), set(). The cycle ends when res.send/json/end is called.',
    example: 'app.post("/api/users", (req, res) => {\n  const { name, email } = req.body;\n  if (!name) return res.status(400).json({ error: "Name required" });\n  res.status(201).json({ id: 1, name, email });\n});',
    keyTakeaways: ['req contains method, url, body, params, query, headers.', 'res methods: json(), send(), status(), set(), end().', 'The cycle ends when a response method is called.', 'Set status codes: 200 (OK), 201 (Created), 400 (Bad Request), 404, 500.'],
    commonMistakes: ['Calling res.send() twice (throws error).', 'Not setting appropriate HTTP status codes.', 'Not validating request body before processing.'],
    whenToUse: 'Understanding the request-response cycle is fundamental to building any API.',
    interviewTip: 'Mention that you always set correct HTTP status codes — it\'s a sign of API quality.',
    tryYourself: 'Create a POST route that validates input, returns 400 for missing fields, and 201 on success.',
  },
  'st-node-8': {
    title: 'Error Handling',
    definition: 'Express error handling uses a special middleware with four arguments (err, req, res, next) to catch and respond to errors.',
    explanation: 'Express distinguishes sync errors (caught automatically) from async errors (must be passed to next()). Async route handlers should use try/catch and pass errors to next(err). Error-handling middleware must be registered last, after all routes. It should log the error and send an appropriate response.',
    example: '// Async handler with error handling\napp.get("/users/:id", async (req, res, next) => {\n  try {\n    const user = await getUser(req.params.id);\n    if (!user) throw new Error("User not found");\n    res.json(user);\n  } catch (err) {\n    next(err);\n  }\n});\n// Error middleware (last!)\napp.use((err, req, res, next) => {\n  res.status(500).json({ error: err.message });\n});',
    keyTakeaways: ['Error middleware has 4 args: (err, req, res, next).', 'Register error middleware last, after all routes.', 'Async errors must be passed to next(err).', 'Always log errors and send user-friendly responses.'],
    commonMistakes: ['Not passing async errors to next().', 'Registering error middleware before routes.', 'Sending stack traces to clients (security risk).'],
    whenToUse: 'Always implement error handling middleware in production Express apps.',
    interviewTip: 'Mention that you use express-async-handler or wrap async routes to avoid try/catch boilerplate.',
    tryYourself: 'Add error handling middleware to an Express app that catches and responds to errors gracefully.',
  },
  'st-node-9': {
    title: 'REST Principles',
    definition: 'REST (Representational State Transfer) is an architectural style for APIs using HTTP methods on resources identified by URLs.',
    explanation: 'REST maps HTTP methods to CRUD: GET (read), POST (create), PUT (update full), PATCH (update partial), DELETE. Resources are nouns: /users, /users/:id. Use proper status codes: 200, 201, 204, 400, 404, 500. REST is stateless — each request contains all needed information. Resources can return links to related resources (HATEOAS).',
    example: 'GET    /api/users       → list all users\nPOST   /api/users       → create a user\nGET    /api/users/:id    → get one user\nPUT    /api/users/:id    → update a user\nDELETE /api/users/:id    → delete a user',
    keyTakeaways: ['REST maps HTTP methods to CRUD operations.', 'Resources are nouns: /users, /products, /orders.', 'Use correct status codes: 200, 201, 204, 400, 404, 500.', 'REST is stateless — each request is independent.'],
    commonMistakes: ['Using verbs in URLs (/getUsers instead of GET /users).', 'Returning wrong status codes (200 for created instead of 201).', 'Not using proper HTTP methods (GET for updates).'],
    whenToUse: 'Use REST for any API that needs to be consumed by multiple clients (web, mobile, third-party).',
    interviewTip: 'Mention that REST resources should be nouns, not verbs — it\'s a common design mistake.',
    tryYourself: 'Design a REST API for a blog with posts and comments, listing all endpoints and methods.',
  },
  'st-node-10': {
    title: 'CRUD Operations',
    definition: 'CRUD (Create, Read, Update, Delete) maps to POST, GET, PUT/PATCH, DELETE — the four basic operations on resources.',
    explanation: 'Each CRUD operation maps to an HTTP method and route. Create (POST) adds a new resource, Read (GET) retrieves one or many, Update (PUT/PATCH) modifies, Delete (DELETE) removes. Validate input on Create/Update, handle not-found on Read/Delete, and return appropriate status codes.',
    example: 'app.post("/items", async (req, res) => { /* create */ });\napp.get("/items", async (req, res) => { /* read all */ });\napp.get("/items/:id", async (req, res) => { /* read one */ });\napp.put("/items/:id", async (req, res) => { /* update */ });\napp.delete("/items/:id", async (req, res) => { /* delete */ });',
    keyTakeaways: ['POST creates, GET reads, PUT/PATCH updates, DELETE removes.', 'Always validate input on create and update operations.', 'Return 201 for created, 200 for read, 204 for deleted.', 'Handle 404 for non-existent resources.'],
    commonMistakes: ['Not validating input before creating/updating.', 'Returning the wrong status code for each operation.', 'Not handling the case where a resource doesn\'t exist.'],
    whenToUse: 'Implement CRUD for every resource in your API.',
    interviewTip: 'Mention that you always validate input and return correct status codes for each CRUD operation.',
    tryYourself: 'Implement full CRUD for an "items" resource with proper validation and status codes.',
  },
  'st-node-11': {
    title: 'API Authentication',
    definition: 'API authentication verifies the identity of clients. Common methods: JWT tokens, API keys, and OAuth 2.0.',
    explanation: 'JWT (JSON Web Token) is the most common: the client sends a token in the Authorization header. The server verifies the token signature and extracts user info. API keys are simpler but less flexible. OAuth 2.0 enables third-party authentication (Google, GitHub). Store tokens securely and use HTTPS.',
    example: '// JWT middleware\nconst auth = (req, res, next) => {\n  const token = req.headers.authorization?.split(" ")[1];\n  try {\n    req.user = jwt.verify(token, SECRET);\n    next();\n  } catch {\n    res.status(401).json({ error: "Unauthorized" });\n  }\n};',
    keyTakeaways: ['JWT tokens are sent in the Authorization header.', 'The server verifies the token signature to authenticate.', 'API keys are simpler but less flexible than JWT.', 'Always use HTTPS to protect tokens in transit.'],
    commonMistakes: ['Storing JWT tokens in localStorage (XSS vulnerable — use httpOnly cookies).', 'Not verifying token expiration.', 'Putting sensitive data in the JWT payload (it\'s not encrypted).'],
    whenToUse: 'Use JWT for stateless authentication, API keys for service-to-service, OAuth for third-party login.',
    interviewTip: 'Mention that JWT payload is base64-encoded, not encrypted — never put sensitive data in it.',
    tryYourself: 'Create a JWT authentication middleware that verifies tokens and attaches the user to the request.',
  },
  'st-node-12': {
    title: 'Environment Variables',
    definition: 'Environment variables store configuration outside code — database URLs, API keys, secrets — using the dotenv package.',
    explanation: 'Create a .env file with key-value pairs. Load it with require("dotenv").config(). Access via process.env.KEY. Never commit .env to git (add to .gitignore). Use different .env files for development, staging, and production. Environment variables keep secrets out of code and enable 12-factor app compliance.',
    example: '// .env file\nDB_URL=mongodb://localhost:27017/mydb\nJWT_SECRET=mysecretkey\nPORT=3000\n\n// app.js\nrequire("dotenv").config();\nconst port = process.env.PORT || 3000;\napp.listen(port);',
    keyTakeaways: ['Environment variables store config outside code.', 'Use dotenv to load .env files.', 'Never commit .env to git.', 'Use different env files for different environments.'],
    commonMistakes: ['Committing .env files with secrets to git.', 'Not using fallback values (process.env.PORT || 3000).', 'Hardcoding secrets in source code.'],
    whenToUse: 'Always use environment variables for configuration and secrets.',
    interviewTip: 'Mention that environment variables are a core principle of the 12-factor app methodology.',
    tryYourself: 'Create a .env file with PORT and DB_URL, load them with dotenv, and use them in an Express app.',
  },
  'st-node-13': {
    title: 'Logging & Monitoring',
    definition: 'Logging records application events for debugging and auditing. Monitoring tracks application health and performance in real-time.',
    explanation: 'Use logging libraries like winston or pino for structured logging. Log levels: error, warn, info, debug. Use middleware like morgan for HTTP request logging. For monitoring, use tools like PM2, New Relic, or Datadog. Always log errors with context (request ID, user ID, timestamp).',
    example: 'const winston = require("winston");\nconst logger = winston.createLogger({\n  level: "info",\n  format: winston.format.json(),\n  transports: [new winston.transports.Console()]\n});\nlogger.info("Server started on port 3000");\nlogger.error("Database connection failed", { error: err.message });',
    keyTakeaways: ['Use structured logging (JSON format) for production.', 'Log levels: error, warn, info, debug.', 'Always include context in error logs (request ID, user).', 'Use monitoring tools for health checks and alerting.'],
    commonMistakes: ['Using console.log in production (no log levels, no structure).', 'Not logging enough context for debugging.', 'Not setting up health check endpoints.'],
    whenToUse: 'Use logging in all production applications. Use monitoring for any production deployment.',
    interviewTip: 'Mention that you use structured JSON logging — it\'s searchable and integrates with log aggregation tools.',
    tryYourself: 'Set up winston logging with info and error levels in an Express app.',
  },
  'st-node-14': {
    title: 'Security Basics',
    definition: 'Backend security includes using helmet for HTTP headers, CORS for cross-origin requests, rate limiting, input validation, and HTTPS.',
    explanation: 'Key security measures: use helmet() to set security headers, configure CORS to allow only trusted origins, use express-rate-limit to prevent brute force, validate and sanitize all input with libraries like Joi or express-validator, use bcrypt for password hashing, and always use HTTPS in production.',
    example: 'const helmet = require("helmet");\nconst cors = require("cors");\nconst rateLimit = require("express-rate-limit");\napp.use(helmet());\napp.use(cors({ origin: "https://myapp.com" }));\napp.use(rateLimit({ max: 100, windowMs: 15 * 60 * 1000 }));',
    keyTakeaways: ['Use helmet() for security headers.', 'Configure CORS to only allow trusted origins.', 'Use rate limiting to prevent brute force attacks.', 'Always validate and sanitize user input.'],
    commonMistakes: ['Leaving CORS open to all origins (origin: "*").', 'Not using rate limiting on authentication endpoints.', 'Storing passwords in plain text (use bcrypt).'],
    whenToUse: 'Apply security best practices to every production backend application.',
    interviewTip: 'Mention OWASP Top 10 — it\'s the standard list of web application security risks.',
    tryYourself: 'Add helmet, CORS with a specific origin, and rate limiting to an Express app.',
  },

  // ═══════════════════════════════════════════════════
  // FULLSTACK — TypeScript
  // ═══════════════════════════════════════════════════
  'st-ts-1': {
    title: 'Types & Interfaces',
    definition: 'Types and interfaces define the shape of data in TypeScript. Interfaces describe object structures; type aliases can describe any type.',
    explanation: 'Interfaces use the interface keyword and are primarily for object shapes. Type aliases use the type keyword and can represent unions, intersections, primitives, and more. Interfaces can be merged (declaration merging); types cannot. For object shapes, interfaces are conventional; for unions and utility types, use type.',
    example: 'interface User {\n  id: number;\n  name: string;\n  email: string;\n}\ntype Status = "active" | "inactive";\ntype ID = string | number;',
    keyTakeaways: ['Interfaces define object shapes and support declaration merging.', 'Type aliases can represent unions, intersections, and primitives.', 'Use interfaces for object shapes; types for unions and utilities.', 'Both can be used as function parameter and return types.'],
    commonMistakes: ['Confusing when to use interface vs type.', 'Not exporting types/interfaces for reuse.', 'Using any instead of proper types.'],
    whenToUse: 'Use interfaces for object shapes and type aliases for unions, intersections, and complex types.',
    interviewTip: 'Mention that interfaces support declaration merging while types don\'t — a subtle but important distinction.',
    tryYourself: 'Create an interface for a Product and a type alias for a union of product categories.',
  },
  'st-ts-2': {
    title: 'Type Annotations',
    definition: 'Type annotations explicitly specify the type of a variable, function parameter, or return value.',
    explanation: 'TypeScript can infer types in many cases, but annotations are needed for function parameters and when you want to be explicit. Use colon syntax: let x: number = 5. For functions, annotate parameters and return types. TypeScript will error if a value doesn\'t match the annotation.',
    example: 'function greet(name: string): string {\n  return `Hello, ${name}`;\n}\nlet count: number = 0;\nlet names: string[] = ["Alice", "Bob"];',
    keyTakeaways: ['Type annotations use colon syntax: variable: type.', 'Function parameters and return types should be annotated.', 'TypeScript can infer many types automatically.', 'Annotations catch type errors at compile time.'],
    commonMistakes: ['Over-annotating when TypeScript can infer the type.', 'Not annotating function return types (can cause subtle bugs).', 'Using any as a type (defeats the purpose of TypeScript).'],
    whenToUse: 'Annotate function parameters and return types. Let TypeScript infer local variables when obvious.',
    interviewTip: 'Mention that you annotate function signatures but let TypeScript infer local variables — a balance of safety and conciseness.',
    tryYourself: 'Write a function with typed parameters and return type that calculates the area of a rectangle.',
  },
  'st-ts-3': {
    title: 'Union & Intersection Types',
    definition: 'Union types (A | B) allow a value to be one of several types. Intersection types (A & B) combine multiple types into one.',
    explanation: 'Union types use the pipe (|) — a value can be any of the listed types. Intersection types use the ampersand (&) — a value must have all properties of both types. Unions are common for optional or polymorphic data. Intersections are used for mixins and combining interfaces.',
    example: 'type ID = string | number;\ninterface Employee { id: number; }\ninterface Manager { reports: number; }\ntype Lead = Employee & Manager; // has both id and reports',
    keyTakeaways: ['Union (|) means "one of" these types.', 'Intersection (&) means "all of" these types.', 'Unions are common for optional or polymorphic values.', 'Intersections combine multiple type requirements.'],
    commonMistakes: ['Not narrowing union types before accessing type-specific properties.', 'Confusing union with intersection semantics.', 'Not using type guards with union types.'],
    whenToUse: 'Use unions for values that can be multiple types. Use intersections to combine multiple type requirements.',
    interviewTip: 'Mention type narrowing — using typeof, instanceof, or in checks to narrow union types.',
    tryYourself: 'Create a union type for a response that can be either a success or error object, and write a function that handles both.',
  },
  'st-ts-4': {
    title: 'Type Assertions',
    definition: 'Type assertions tell TypeScript to treat a value as a specific type, overriding its inferred type.',
    explanation: 'Type assertions use the as keyword: value as Type. They don\'t change the runtime value — they only affect the type system. Use them when you know more about a type than TypeScript can infer (e.g., DOM elements, API responses). Avoid assertions to any; use unknown and narrow instead.',
    example: 'const input = document.getElementById("name") as HTMLInputElement;\ninput.value = "Alice";\n\n// Better: use type guard\nconst el = document.getElementById("name");\nif (el instanceof HTMLInputElement) {\n  el.value = "Alice";\n}',
    keyTakeaways: ['Type assertions use the as keyword.', 'They override TypeScript\'s inferred type but don\'t change runtime behavior.', 'Common for DOM elements and API responses.', 'Avoid asserting to any — use unknown and narrow instead.'],
    commonMistakes: ['Using assertions to suppress type errors instead of fixing them.', 'Asserting to any (loses all type safety).', 'Not using type guards when they\'d be safer than assertions.'],
    whenToUse: 'Use type assertions when you know the type but TypeScript can\'t infer it, like DOM elements or parsed JSON.',
    interviewTip: 'Mention that type assertions don\'t change runtime behavior — they\'re a compile-time hint, not a cast.',
    tryYourself: 'Use a type assertion to get an HTMLInputElement from getElementById and set its value.',
  },
  'st-ts-5': {
    title: 'Generics',
    definition: 'Generics are a way to create reusable components and functions that work with multiple types while maintaining type safety.',
    explanation: 'Generics use type parameters (often T, U, K) that are specified when the function or class is used. They let you write code that works with any type while preserving the relationship between input and output types. Common in collections, utility functions, and React components.',
    example: 'function identity<T>(value: T): T {\n  return value;\n}\nconst num = identity<number>(42);    // type: number\nconst str = identity("hello");       // type: string (inferred)\n\nfunction getFirst<T>(arr: T[]): T {\n  return arr[0];\n}',
    keyTakeaways: ['Generics use type parameters (T, U, K) for reusable, type-safe code.', 'They preserve the relationship between input and output types.', 'Type can be explicitly provided or inferred from usage.', 'Common in utility functions, collections, and React components.'],
    commonMistakes: ['Using any instead of generics (loses type safety).', 'Not constraining generics with extends when needed.', 'Overcomplicating with too many type parameters.'],
    whenToUse: 'Use generics when a function or component should work with multiple types while maintaining type relationships.',
    interviewTip: 'Mention that generics preserve type relationships — identity<T> returns the same type it receives, unlike any.',
    tryYourself: 'Write a generic function that takes an array and returns the first element, preserving the element type.',
  },
  'st-ts-6': {
    title: 'Utility Types',
    definition: 'TypeScript provides built-in utility types that transform existing types: Partial, Pick, Omit, Record, Readonly, and more.',
    explanation: 'Common utilities: Partial<T> makes all properties optional, Pick<T, Keys> selects specific properties, Omit<T, Keys> removes properties, Record<K, V> creates a key-value map type, Readonly<T> makes all properties readonly. These reduce boilerplate and ensure type safety when transforming types.',
    example: 'interface User { id: number; name: string; email: string; }\ntype UserPreview = Pick<User, "id" | "name">;\ntype UserUpdate = Partial<Omit<User, "id">>;\ntype UserMap = Record<string, User>;',
    keyTakeaways: ['Partial<T> makes all properties optional.', 'Pick<T, K> selects specific properties from T.', 'Omit<T, K> removes specific properties from T.', 'Record<K, V> creates a key-value map type.'],
    commonMistakes: ['Not knowing about utility types and writing redundant type definitions.', 'Using Pick/Omit with incorrect key types.', 'Not combining utilities (e.g., Partial<Omit<T, "id">>).'],
    whenToUse: 'Use utility types to transform existing types instead of creating new interfaces from scratch.',
    interviewTip: 'Mention Partial<Omit<T, "id">> for update DTOs — it\'s a common real-world pattern.',
    tryYourself: 'Use Pick to create a UserPreview type with only id and name from a User interface.',
  },
  'st-ts-7': {
    title: 'Decorators',
    definition: 'Decorators are a TypeScript feature for adding metadata or behavior to classes, methods, properties, and parameters using @decorator syntax.',
    explanation: 'Decorators are functions prefixed with @ that modify classes or their members. Common in NestJS and TypeORM. Types: class decorators, method decorators, property decorators, parameter decorators. They enable declarative patterns for logging, validation, dependency injection, and routing.',
    example: 'function Log(target: any, key: string) {\n  console.log(`Method ${key} called`);\n}\nclass MyClass {\n  @Log\n  greet() { return "hello"; }\n}',
    keyTakeaways: ['Decorators use @decorator syntax to modify classes and members.', 'Types: class, method, property, and parameter decorators.', 'Common in frameworks like NestJS and TypeORM.', 'Enable declarative patterns for cross-cutting concerns.'],
    commonMistakes: ['Not enabling experimentalDecorators in tsconfig.', 'Confusing decorator order of execution.', 'Using decorators where simple functions would suffice.'],
    whenToUse: 'Use decorators when working with frameworks that support them (NestJS, TypeORM) or for declarative metadata.',
    interviewTip: 'Mention that decorators are an experimental feature — they\'re widely used in NestJS but not yet standard JavaScript.',
    tryYourself: 'Create a simple method decorator that logs when a method is called.',
  },
  'st-ts-8': {
    title: 'Typing Props & State',
    definition: 'In React with TypeScript, props and state are typed using interfaces or type aliases for compile-time safety.',
    explanation: 'Define prop types with interfaces extending React.PropsWithChildren or using type aliases. Use useState with a generic: useState<string>(""). For events, use React.ChangeEvent, React.MouseEvent. For refs, use useRef<HTMLInputElement>. TypeScript catches prop and state type errors at compile time.',
    example: 'interface ButtonProps {\n  label: string;\n  onClick: () => void;\n  disabled?: boolean;\n}\nfunction Button({ label, onClick, disabled }: ButtonProps) {\n  return <button onClick={onClick} disabled={disabled}>{label}</button>;\n}',
    keyTakeaways: ['Define prop types with interfaces or type aliases.', 'Use useState<T> for typed state.', 'Use React event types: ChangeEvent, MouseEvent.', 'Optional props use the ? modifier.'],
    commonMistakes: ['Not typing event handlers properly.', 'Using any for props instead of proper interfaces.', 'Forgetting to make optional props optional with ?.'],
    whenToUse: 'Always type props and state in React + TypeScript projects.',
    interviewTip: 'Mention that you always type props with interfaces — it catches bugs before runtime.',
    tryYourself: 'Create a typed React component with required and optional props.',
  },
  'st-ts-9': {
    title: 'Typing Events & Refs',
    definition: 'React events and refs have specific TypeScript types that ensure type-safe event handling and DOM access.',
    explanation: 'Event types: React.ChangeEvent<HTMLInputElement> for input changes, React.MouseEvent<HTMLButtonElement> for clicks, React.FormEvent<HTMLFormElement> for form submissions. Refs: useRef<HTMLDivElement>(null) for DOM elements. Always type events and refs to get autocompletion and type checking.',
    example: 'function Form() {\n  const inputRef = useRef<HTMLInputElement>(null);\n  const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {\n    console.log(e.target.value);\n  };\n  return <input ref={inputRef} onChange={handleChange} />;\n}',
    keyTakeaways: ['Use React.ChangeEvent, MouseEvent, FormEvent for event types.', 'Use useRef<T>(null) for typed DOM refs.', 'Event types include the element type as a generic.', 'Typing events gives autocompletion for target, value, etc.'],
    commonMistakes: ['Using React.FormEvent without the element generic.', 'Not typing useRef, leading to null errors.', 'Using any for event types.'],
    whenToUse: 'Always type events and refs in React + TypeScript for type safety and autocompletion.',
    interviewTip: 'Mention that typing events gives you autocompletion for e.target.value — a productivity boost.',
    tryYourself: 'Create a form with typed onChange and onSubmit handlers in TypeScript.',
  },

  // ═══════════════════════════════════════════════════
  // FULLSTACK — Projects & Deployment
  // ═══════════════════════════════════════════════════
  'st-fsproj-1': {
    title: 'Project Structure',
    definition: 'A full stack project structure organizes frontend, backend, and shared code into a maintainable architecture.',
    explanation: 'Common structures: monorepo (frontend and backend in one repo with shared types), or separate repos. A typical monorepo: /client (React frontend), /server (Node.js backend), /shared (types, schemas). Use tools like Turborepo or Nx for monorepo management. Clear separation of concerns enables independent scaling and deployment.',
    example: '/my-app\n  /client     → React frontend\n  /server     → Node.js API\n  /shared     → TypeScript types, validation schemas\n  package.json → workspace config',
    keyTakeaways: ['Monorepos keep frontend and backend together with shared code.', 'Separate /client, /server, and /shared directories.', 'Shared types prevent frontend/backend type mismatches.', 'Use Turborepo or Nx for monorepo management.'],
    commonMistakes: ['Mixing frontend and backend code in the same directory.', 'Not sharing types between frontend and backend.', 'Overcomplicating the structure for a small project.'],
    whenToUse: 'Establish project structure at the start of any full stack project.',
    interviewTip: 'Mention that shared types between frontend and backend prevent integration bugs — a key monorepo benefit.',
    tryYourself: 'Design a folder structure for a full stack todo app with React frontend and Node.js backend.',
  },
  'st-fsproj-2': {
    title: 'Frontend-Backend Integration',
    definition: 'Frontend-backend integration connects the React frontend to the Node.js API via HTTP requests, handling loading, error, and success states.',
    explanation: 'Use fetch or axios for API calls. Handle three states: loading (show spinner), error (show message), success (show data). Use React Query or SWR for caching, refetching, and optimistic updates. Always type API responses with TypeScript. Use environment variables for API URLs.',
    example: 'const [data, setData] = useState(null);\nconst [loading, setLoading] = useState(true);\nuseEffect(() => {\n  fetch(`${import.meta.env.VITE_API_URL}/users`)\n    .then(r => r.json())\n    .then(setData)\n    .finally(() => setLoading(false));\n}, []);',
    keyTakeaways: ['Handle loading, error, and success states for all API calls.', 'Use React Query or SWR for caching and refetching.', 'Type API responses with TypeScript interfaces.', 'Use environment variables for API base URLs.'],
    commonMistakes: ['Not handling loading and error states.', 'Hardcoding API URLs instead of using env vars.', 'Not typing API responses.'],
    whenToUse: 'Integrate frontend and backend whenever the frontend needs to display or modify server data.',
    interviewTip: 'Mention React Query for data fetching — it handles caching, refetching, and loading states automatically.',
    tryYourself: 'Create a React component that fetches and displays data from an API with loading and error states.',
  },
  'st-fsproj-3': {
    title: 'Database Integration',
    definition: 'Database integration connects the backend to a database, using an ORM (Prisma, TypeORM) or raw SQL queries.',
    explanation: 'ORMs like Prisma provide type-safe database access with auto-generated types. Raw SQL with pg or mysql2 gives more control. Use connection pooling for performance. Handle migrations for schema changes. Always use transactions for multi-step operations. Separate database logic into repository modules.',
    example: '// Prisma example\nimport { PrismaClient } from "@prisma/client";\nconst prisma = new PrismaClient();\nconst users = await prisma.user.findMany();\n\n// Raw SQL\nconst result = await pool.query("SELECT * FROM users WHERE id = $1", [id]);',
    keyTakeaways: ['ORMs (Prisma, TypeORM) provide type-safe database access.', 'Raw SQL gives more control but less type safety.', 'Use connection pooling for performance.', 'Use migrations for schema changes.'],
    commonMistakes: ['Not using connection pooling (exhausts connections).', 'SQL injection from not using parameterized queries.', 'Not running migrations in deployment.'],
    whenToUse: 'Integrate a database whenever the application needs to persist data.',
    interviewTip: 'Mention Prisma for type-safe database access — it\'s modern and popular in the Node.js ecosystem.',
    tryYourself: 'Set up a simple Prisma schema with a User model and query users from an Express route.',
  },
  'st-fsproj-4': {
    title: 'Build & Bundle',
    definition: 'Build and bundle is the process of compiling and optimizing frontend and backend code for production deployment.',
    explanation: 'Frontend: Vite or webpack bundles React into static files (HTML, CSS, JS). Backend: TypeScript compiles to JavaScript, or uses ts-node for dev. Tree-shaking removes unused code. Code-splitting creates smaller chunks loaded on demand. Minification reduces file sizes. Source maps enable debugging in production.',
    example: '// Vite build\nvite build → creates /dist with optimized files\n\n// TypeScript build\ntsc → compiles .ts to .js in /dist\n\n// Package.json scripts\n"build:client": "cd client && npm run build"\n"build:server": "tsc -p server/tsconfig.json"',
    keyTakeaways: ['Frontend build tools: Vite, webpack bundle into static files.', 'Backend: TypeScript compiles to JavaScript.', 'Tree-shaking removes unused code.', 'Code-splitting creates smaller chunks for lazy loading.'],
    commonMistakes: ['Not minifying production builds.', 'Including source maps in production (can expose code).', 'Not tree-shaking, leading to large bundles.'],
    whenToUse: 'Build and bundle before every production deployment.',
    interviewTip: 'Mention that you use Vite for frontend builds — it\'s faster than webpack and the modern standard.',
    tryYourself: 'Configure a Vite build for a React app and verify the output in the dist folder.',
  },
  'st-fsproj-5': {
    title: 'Docker for Full Stack',
    definition: 'Docker containerizes both frontend and backend into portable, reproducible environments using Dockerfiles and Docker Compose.',
    explanation: 'Create a Dockerfile for each service (frontend, backend, database). Use Docker Compose to run all services together. Multi-stage builds reduce image size. Use .dockerignore to exclude node_modules and dist. Environment variables configure each container. Docker ensures the app runs identically across environments.',
    example: '# Dockerfile (backend)\nFROM node:18-alpine\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci --production\nCOPY . .\nCMD ["node", "dist/index.js"]\n\n# docker-compose.yml\nservices:\n  api: build: ./server\n  web: build: ./client\n  db: image: postgres:16',
    keyTakeaways: ['Create a Dockerfile for each service.', 'Docker Compose runs multiple services together.', 'Multi-stage builds reduce image size.', 'Use .dockerignore to exclude unnecessary files.'],
    commonMistakes: ['Not using multi-stage builds (large images).', 'Copying node_modules into the image.', 'Not using .dockerignore.'],
    whenToUse: 'Use Docker for any production deployment to ensure consistency across environments.',
    interviewTip: 'Mention multi-stage builds — they separate build dependencies from runtime, reducing image size significantly.',
    tryYourself: 'Create a Dockerfile for a Node.js backend and a docker-compose.yml that runs the API and a PostgreSQL database.',
  },
  'st-fsproj-6': {
    title: 'CI/CD Pipelines',
    definition: 'CI/CD (Continuous Integration/Continuous Deployment) pipelines automate testing, building, and deploying code on every push.',
    explanation: 'CI runs tests and builds on every push to ensure code quality. CD deploys to staging or production automatically. GitHub Actions, GitLab CI, and Jenkins are common tools. A typical pipeline: lint → test → build → deploy. Use environment secrets for deployment credentials. Run parallel jobs for speed.',
    example: '# .github/workflows/deploy.yml\nname: Deploy\non: [push]\njobs:\n  build:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - run: npm ci\n      - run: npm test\n      - run: npm run build\n      - run: vercel deploy --prod',
    keyTakeaways: ['CI runs tests and builds on every push.', 'CD deploys automatically after successful CI.', 'Common tools: GitHub Actions, GitLab CI.', 'Pipeline: lint → test → build → deploy.'],
    commonMistakes: ['Not running tests in CI.', 'Deploying without testing first.', 'Not using secrets for deployment credentials.'],
    whenToUse: 'Use CI/CD for any project that needs automated testing and deployment.',
    interviewTip: 'Mention that CI/CD catches integration bugs early and enables frequent, safe deployments.',
    tryYourself: 'Create a GitHub Actions workflow that runs tests and builds a project on push.',
  },
  'st-fsproj-7': {
    title: 'Cloud Deployment',
    definition: 'Cloud deployment hosts the application on cloud platforms like Vercel, Netlify, AWS, or Heroku, making it accessible to users.',
    explanation: 'Frontend: deploy to Vercel or Netlify (static hosting with CDN). Backend: deploy to Render, Railway, or AWS EC2/ECS. Database: use managed services like Supabase, PlanetScale, or AWS RDS. Use custom domains, SSL certificates, and environment variables. Monitor uptime and performance after deployment.',
    example: 'Frontend: vercel deploy → auto-deployed to CDN\nBackend: render deploy → Docker container on Render\nDatabase: Supabase → managed PostgreSQL\nCustom domain: myapp.com → Vercel DNS',
    keyTakeaways: ['Frontend: Vercel, Netlify for static hosting with CDN.', 'Backend: Render, Railway, AWS for server deployment.', 'Database: Supabase, PlanetScale, AWS RDS for managed databases.', 'Always set up custom domains and SSL.'],
    commonMistakes: ['Not setting environment variables in the cloud platform.', 'Forgetting to configure CORS for the deployed backend.', 'Not monitoring uptime and performance.'],
    whenToUse: 'Deploy to the cloud when the application is ready for users to access.',
    interviewTip: 'Mention that you choose deployment platforms based on project needs — Vercel for frontend, Render for backend, Supabase for database.',
    tryYourself: 'Deploy a React frontend to Vercel and a Node.js backend to Render.',
  },
  'st-fsproj-8': {
    title: 'Project Showcase',
    definition: 'Project showcase is presenting your full stack project to employers through a live demo, README, and portfolio.',
    explanation: 'A good showcase includes: a live demo URL, a well-written README with screenshots, a short demo video, and a description of the tech stack and architecture. Highlight what makes the project interesting — unique features, challenges solved, or performance optimizations. Make it easy for recruiters to understand and run.',
    example: 'README structure:\n# Project Name\n## Live Demo: [URL]\n## Screenshots\n## Tech Stack\n## Features\n## Architecture\n## How to Run\n## What I Learned',
    keyTakeaways: ['Include a live demo URL for instant access.', 'Write a clear README with screenshots and architecture.', 'Record a short demo video (2-3 minutes).', 'Highlight unique features and challenges solved.'],
    commonMistakes: ['No live demo (employers won\'t clone and run locally).', 'Poorly written or missing README.', 'Not explaining the project\'s purpose and features.'],
    whenToUse: 'Showcase projects when job hunting or presenting to potential employers.',
    interviewTip: 'Mention that you always include a live demo URL — recruiters rarely clone repos to run projects.',
    tryYourself: 'Write a compelling README for a full stack project with live demo link, screenshots, and tech stack.',
  },
  'st-fsproj-9': {
    title: 'README & Documentation',
    definition: 'A README is the front page of a project, explaining what it is, how to run it, and its key features.',
    explanation: 'A good README includes: project title and description, live demo link, screenshots/GIF, tech stack, features list, installation instructions, environment setup, scripts, and contribution guidelines. Use clear formatting with headers, lists, and code blocks. Include badges for build status and license.',
    example: '# Task Manager App\n\n![Demo](demo.gif)\n\nA full-stack task management app built with React, Node.js, and PostgreSQL.\n\n## Features\n- User authentication\n- CRUD operations\n- Real-time updates\n\n## Tech Stack\n- Frontend: React, TypeScript, Tailwind\n- Backend: Node.js, Express, Prisma\n- Database: PostgreSQL',
    keyTakeaways: ['Include project title, description, and live demo link.', 'Add screenshots or GIFs to showcase the UI.', 'List features, tech stack, and installation steps.', 'Use clear formatting with headers and code blocks.'],
    commonMistakes: ['No installation instructions.', 'No screenshots or visual examples.', 'Outdated documentation that doesn\'t match the code.'],
    whenToUse: 'Always write a README for any project you plan to share or showcase.',
    interviewTip: 'Mention that a good README is often the first thing employers look at — it\'s your project\'s first impression.',
    tryYourself: 'Write a complete README for a project with all key sections.',
  },
  'st-fsproj-10': {
    title: 'Open Source Contribution',
    definition: 'Open source contribution is participating in public projects by fixing bugs, adding features, or improving documentation.',
    explanation: 'Start by finding beginner-friendly issues (labeled "good first issue"). Fork the repo, create a branch, make changes, and submit a pull request. Read the CONTRIBUTING.md file. Follow the project\'s code style. Write clear PR descriptions. Be responsive to maintainer feedback. Contributions demonstrate collaboration skills.',
    example: '1. Find a "good first issue" on GitHub\n2. Fork the repository\n3. Create a branch: git checkout -b fix-typo\n4. Make changes and commit\n5. Push and open a Pull Request\n6. Respond to review feedback',
    keyTakeaways: ['Start with "good first issue" labeled issues.', 'Follow the project\'s CONTRIBUTING.md guide.', 'Write clear PR descriptions explaining what and why.', 'Be responsive to maintainer feedback.'],
    commonMistakes: ['Not reading the contributing guidelines.', 'Making large changes without discussion first.', 'Not writing a clear PR description.'],
    whenToUse: 'Contribute to open source to build skills, network, and demonstrate collaboration ability.',
    interviewTip: 'Mention that open source contributions show you can work with existing codebases and collaborate — highly valued by employers.',
    tryYourself: 'Find a beginner-friendly issue on GitHub, fork the repo, and submit a small fix.',
  },
};
